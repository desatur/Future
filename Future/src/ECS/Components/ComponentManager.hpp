#pragma once
#include <vector>
#include <unordered_map>
#include <bitset>
#include <memory>
#include <typeindex>

namespace Future {
    class ComponentManager {
    public:
        ComponentManager();
        ~ComponentManager();

    private:

    };
}
