#pragma once
#include "../../Core/src/Export.hpp"

namespace Future {
    class FUTURE_API Rendering {
        public:
            Rendering();
            ~Rendering();
            void Initialize();
        };
}
